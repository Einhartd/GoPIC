# Function: unlikely.__tls_init
# Mangled Symbol: unlikely.__tls_init
# Architecture: AMD Zen 4 (znver4) AVX-512 / -O3

.section .text.unlikely.__tls_init
	.size	__tls_init.cold, .-__tls_init.cold
.LCOLDE179:
	